/**
 * This script is injected dynamically into the page
 * when the user clicks in the popup, so by the time
 * we activate it, we can immediately analyze DOM.
 */

document.body.style.background = "orange";
